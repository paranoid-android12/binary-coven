# Supabase Configuration
# Get these values from your Supabase project settings at https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (for admin operations - KEEP SECRET!)
# This key bypasses Row Level Security - only use in server-side code
# Get from: Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin Configuration
# This is the hashed password for admin access (generate using bcrypt)
# Example: for password "admin123", use bcrypt to generate the hash
ADMIN_PASSWORD_HASH=$2a$10$example.hash.here

# Optional: Admin password in plain text (will be hashed on first use)
# Only use this for initial setup, then remove and use ADMIN_PASSWORD_HASH
ADMIN_PASSWORD=changeme123
