# Password Validation
# Set to 'true' to disable password validation (allows any password)
# Set to 'false' or remove to enable password validation (requires uppercase, number, special char)
NEXT_PUBLIC_DISABLE_PASSWORD_VALIDATION=false

# Add other environment variables below as needed
